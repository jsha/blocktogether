  {{>header.mustache}}
  Block actions pending and performed on your account by Block Together.
  "Cancelled-duplicate" means you already blocked the target.
  <table class='table table-condensed actions'>
  <thead>
    <tr class='table-header'>
      <th>Action</th>
      <th>Target</th>
      <th>Status</th>
      <th>Created</th>
      <th>Updated</th>
    </tr>
  </thead>
  {{#actions}}
    <tr class='action' data-id='{{id}}'>
      <td>{{type}}</td>
      <td>
        <a href='https://twitter.com/account/redirect_by_id/{{sink_uid}}'
          >{{sink_uid}}</a>
      </td>
      <td>{{status}}</td>
      <td title="{{createdAt}}">{{prettyCreated}}</td>
      <td title="{{updatedAt}}">{{prettyUpdated}}</td>
    </tr>
  {{/actions}}
  </table>
  {{>footer.mustache}}
